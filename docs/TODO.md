# Chefing — Todo

States: `open` · `doing` · `blocked` · `done`

| id | title | state | descr | dependencies |
|----|-------|-------|-------|--------------|
| T01 | MVP Rührteig-Kombinator | done | Generisches Schema, Regeln in `operations.json`, Korpus (3×7 Rezepte), Morph-Kasten, Ablaufdiagramm, Rezeptkarte, Statistik. Commit `c809b84`. | – |
| T02 | Push nach GitHub | done | `c809b84` und `f667e53` sind auf `origin/main`. Nutzer hat SSH-Remote eingerichtet und gepusht. | T01 |
| T03 | README | open | Kurzbeschreibung, Start (`npx serve`), Datenmodell-Übersicht, wie neue Archetypen/Korpus ergänzt werden. | T01 |
| T04 | Korpus erweitern | open | Mehr Quellrezepte je Variante (Ziel ≥ 15), damit Median/IQR belastbar werden. Quellen: gutekueche.at, oetker.de, lecker.de, kuechengoetter.de (chefkoch blockiert Fetch). | T01 |
| T05 | Varianten-Cluster in Statistik | open | Korpus nach Fett-Typ × Säure × Einlage clustern und im Stats-Panel zeigen (war im Plan, noch nicht umgesetzt). | T04 |
| T06 | Zweiter Archetyp als reine Daten | open | Z. B. „Gemüse braten“ oder „Hefeteig“ — nur JSON ergänzen, keine JS-Sonderlogik. `rules.js` referenziert noch `archetypes.ruehrteig` hart (Zeilen 104, 112) → generalisieren. Baut auf T08 (Kategorien) auf. | T08 |
| T07 | Tests headless | open | `check.mjs` aus dem Scratchpad ins Repo (`test/`) übernehmen und um Regel-Fälle (u. a. `strenge`) erweitern. | T01, T08 |
| T08 | Kategorien + Verben + Strenge-Grade (DR-008) | done | `kategorien.json` (10 Kategorien, Icons), `verben.json` (5 Verben, Icons), `ingredients.json`/`operations.json` erweitert, `rules.js` prüft `strenge` (hart/empfohlen/frei), Icons in Morph-Kasten + Ablaufdiagramm. Verifiziert: Öl + All-in-one jetzt gültig mit Empfehlungs-Hinweis (vorher hart blockiert); Öl + Creme weiterhin ungültig (harte Regel). | T01 |
| T09 | Operationen echt auf Verb+Intensität verdichten | done | Umgesetzt (DR-014, 2. Nachtrag): `verb` auf 4 Werte reduziert (mischen/sieben/formen/garen), `intensitaet` (passiv/sanft/kraeftig) + `verteilung` (einzeln/abwechselnd) als neue kleine Attribute auf `verb: mischen`. 6 methodenspezifische Operationen (aufschlagen, eier_einzeln, eier_zucker_schaumig, trocken_mischen, unterheben_kurz, alles_verruehren) aus `operations.json` entfernt und inline in die jeweilige `methods.json`-`sequenz` verschoben (`sequenz`-Einträge dürfen jetzt String oder Objekt sein). `operations.json` von 13 auf 7 benannte Operationen geschrumpft. `rules.js` löst beide Formen gleich auf; `engine.js`/`flow.js` unverändert bis auf 2 durchgereichte Felder + Icon-Ableitungstabelle. Alle 4 Methoden im Browser verifiziert. | T08 |
| T10 | Strenge auf Zutat-Ebene (Rollen) | open | Substitutionsfreiheit auch für Zutat-Wahl markieren (z. B. Einlage-Obst frei austauschbar, Struktur-Mehl strikt), nicht nur für Operationen. | T08 |
| T11 | Piktogramme statt Emoji + Gemüse/Obst-Split + Schritt = Zutat+Verb (DR-009) | done | `src/ui/icons.js` (handgezeichnete Formen, HTML- und SVG-Renderer), `gemuese`/`obst` getrennt, Ablaufschritt zeigt Zutat-Kategorie-Icons + Verb-Icon kombiniert. Im Browser verifiziert (Icon-Vorschau-Seite + App, alle drei Presets, keine Konsolenfehler). | T08 |
| T12 | Icon-Feinschliff: S-Twist, Steak, eigenes Whisking-Verb | done | `mischen` = S-Twist-Bogen, `fleisch` = Steak mit ausgesparten Grillstreifen (evenodd), neues Verb `schlagen` (Schneebesen) für aufschlagen/verquirlen/eier_zucker_schaumig. Verifiziert per Icon-Vorschau + App-Ablaufdiagramm. Siehe DR-009-Nachtrag. | T11 |
| T13 | Eigenes „Sieben“-Verb für trockenes Mischen | done | Siebtes Verb `sieben` (Sieb-Ring + durchfallendes Pulver) für `trocken_mischen`, statt des generischen `mischen`-S. Verifiziert per Icon-Vorschau + Ablaufdiagramm. Siehe DR-009-Nachtrag 2. | T12 |
| T14 | Regression: Gluten-Warnung prüfte falsches Feld | done | `rules.js` prüfte `kategorie !== 'getreide'`, das seit DR-008 bei allen Struktur-Optionen „mehl“ ist → Warnung feuerte immer, auch bei reinem Weizenmehl. Fix: neues Feld `bildet_gluten` je Zutat, Warnung präzisiert (Bindung vs. Körper, mit Korpus-Beispiel). Verifiziert: Weizenmehl ohne Warnung, Speisestärke mit korrektem Text. Siehe DR-010. | T08 |
| T15 | Multi-Zutat je Rolle mit Anteilen (Option B, DR-012) | done | `state.zutaten[rolle]` ist jetzt `[{zutat, anteil}]` statt einer einzelnen Zutat — für ALLE Rollen (Nutzer mischt z. B. Fett aus Butter+Öl). `propsOf` blendet Eigenschaften (dominante Zutat + Oder-Verknüpfung für saeure/bildet_gluten/quellfaehig/trieb_typ), `engine.js` verteilt die Rollen-Bäckerprozent nach Anteilen auf mehrere Zeilen, `morphbox.js` erlaubt Mehrfachauswahl mit Anteil-Eingabe. Ersetzt die enger gefasste Rollen-Split-Idee aus DR-011. | T14 |
| T16 | Quellen-Log für Web-Recherche | done | `docs/QUELLEN.md` angelegt, rückwirkend befüllt (11 Domains, 21 Seiten aus dem bestehenden Korpus). Konvention als Memory hinterlegt: jede künftige Recherche wird dort angehängt (pro Domain, nicht pro Einzelseite). | T04 |
| T17 | Deploy per `deploy.php` auf eigenes Hosting (DR-013) | done | `deploy.php` + `chefing.cfg` live auf hamsch.net, `.htaccess` blockt `*.cfg` (403 verifiziert), Deploy-Aufruf getestet (22 Dateien geschrieben, 0 Fehler). | – |
| T18 | Icon-Set für Rühr-Verben konsolidieren (mix in, blend, unterheben) | done | Umgesetzt als KISS-Variante (DR-014, Nachtrag): `zusatzverben`-Feld in `operations.json`, `unterheben_abwechselnd` trägt `zusatzverben: ["sieben", "wiederholen"]` — ein Schritt, drei Icons, statt separatem Sieben- oder Wiederholen-Schritt. `muffin`/`allinone` unverändert. Im Browser verifiziert (beide Methoden, keine Konsolenfehler). | – |
| T19 | Icon-Set überarbeiten: freies Icon-Set prüfen + eigene Nacharbeiten + Outline-Variante | open | Nutzer-Feedback: (1) prüfen, ob ein freies/offenes Icon-Set (Lizenz-Check nötig) bereits alle unsere Schlüssel abdeckt, statt jedes Icon in `src/ui/icons.js` selbst zu zeichnen. (2) Gezielte Nacharbeiten unabhängig davon: `salz` leichter/luftiger wirken lassen (~3 Punkte statt Salzstreuer-Form), `suesse` als Stapel Zuckerwürfel statt der aktuellen vier Quadrate, `garen` komplett neu (aktuelle Flammen-Form unklar). (3) Zusätzliche helle/Outline-Variante („holo“, nur Umriss statt Vollfläche) fürs Icon-Set, z. B. für Dark-Mode oder sekundäre Kontexte. | T11 |
