# Chefing — Todo-Archiv

Abgeschlossene (`done`) Einträge aus `docs/TODO.md`. Konvention: sobald ein Todo auf
`done` gesetzt wird, wandert die Zeile hierher — neueste Archivierung immer **oben**
eingefügt (nicht ans Ende anhängen). `docs/TODO.md` bleibt dadurch kurz und zeigt nur
noch aktive Arbeit.

| id | title | state | descr | dependencies |
|----|-------|-------|-------|--------------|
| T23 | Erfahrungs-Regeln aus `engine.js` in Daten überführen | done | Abhängigkeit von T04 als zu eng gefasst erkannt und korrigiert (siehe DR-019-Nachtrag) — T23 verlangt nur die drei bereits bekannten, fachlich begründeten Anpassungen als Daten-Regeln, nicht die (weiterhin auf T04 wartende) automatische Korpus-Korrelationssuche, dafür neu als T32 registriert. Neue Datei `src/data/erfahrungsregeln.json` (Schema wie `operations.json`s `voraussetzung`: `wenn`-Bedingung gegen geblendete Zutat-Eigenschaften, dazu `voraussetzung_quelle`/`wenn_korpus_fehlt` für die Öl-Regel, die auf tatsächliche Korpus-Deckung prüft) ersetzt die if/else-Kette in `engine.js:34-47`. Generische `wendeErfahrungsregelnAn()` wendet pro Ziel-Rolle nur die erste passende Regel in Deklarationsreihenfolge an (bildet die bisherige Natron-vor-Säure-Priorität nach, sonst würden sich beide Faktoren multiplikativ überlagern). `begruendung` unterstützt `{rolle}`-Platzhalter für die dynamische Zutat-Erwähnung (z. B. „Saure Sahne ist sauer..."). `pruefeBedingung` aus `rules.js` exportiert und wiederverwendet statt dupliziert. Verifiziert per Node-Skript (13 Fälle, inkl. exakter bp-Werte und Korpus-Deckungslücken-Logik) und im Browser gegen den echten, gewachsenen Korpus (der inzwischen bereits ein Öl-Rezept enthält — Fallback-Regel greift dort korrekt NICHT, identisch zum alten Code bei denselben Daten) sowie Natron- und Säure-Pfad einzeln bestätigt — keine Konsolenfehler, keine Regression (37 bisherige Node-Checks weiterhin grün). | — |
| T31 | Pin auch pro Zutat, nicht nur pro Rolle | done | `state.fixiert` von `{[rolle]: true}` auf `{rollen: {[rolle]: true}, zutaten: {[rolle]: {[zutat]: true}}}` umgestellt (main.js, morphbox.js, rules.js — bewusste Erweiterung des T24-Schemas). Pin-Icon je Zutat-Option (nur bei bereits gemischter Rolle und tatsächlich gewählter Zutat) erscheint per CSS nur bei `:hover`/`:focus-visible`/aktiv (`.opt-pin` in style.css), als `<span role="button">` statt verschachteltem `<button>` (in mehreren Browsern nicht zuverlässig klickbar), mit `stopPropagation()` wie beim bestehenden Anteil-Eingabefeld. `rules.js`: `rolleIstGesperrt()` prüft Rollen- ODER Zutat-Pin (eine Kaskade, T26, ersetzt immer die ganze Rolle und würde eine fixierte Zutat sonst verdrängen); `ermittleFixierung()` identifiziert präzise die für eine Bedingung tatsächlich *verantwortliche* Zutat — bei „wenn"-Bedingungen die, die die Guard-Eigenschaft auslöst (z. B. Natron für `trieb_typ=chemisch_basisch`, nicht das zufällig mitgepinnte Backpulver), sonst die von `blendProps` dominante Zutat. Verifiziert per Node-Skript (8 neue Fälle: falsche Zutat gepinnt → kein Hinweis, richtige Zutat gepinnt → Hinweis nennt sie namentlich, Zutat in Kaskaden-Zielrolle gepinnt → kein Vorschlag, Zutat in unbeteiligter Rolle gepinnt → Vorschlag bleibt, leeres Pin-Objekt → wie ungepinnt) und im Browser (Butter+Öl-Mix: Pin erscheint nur bei Hover, Klick pinnt ohne die Auswahl zu verändern; Backpulver+Natron-Mix: Backpulver gepinnt → kein Hinweis, Natron gepinnt → „Natron“ (in „Trieb“) genannt, zusätzlich Wasser in Flüssigkeit gepinnt → Kaskaden-Vorschlag verschwindet korrekt) — keine Konsolenfehler, keine Regression (37 Node-Checks über T22/T24/T25/T26/T31 weiterhin grün). | T24 |
| T30 | Multi-Select-Interaktion umkehren + Hinweistext als Fußnote | done | Der Hinweis „Mehrere Zutaten gleichzeitig anklicken …“ ist aus jeder Rolle in `morphbox.js` entfernt und steht jetzt einmalig als zweiter Absatz im `<footer>` (`index.html`). Klick-Verhalten umgekehrt: ein Klick auf eine Zutat-Option ersetzt jetzt die Auswahl der Rolle (`onClick` → `[{zutat, anteil:1}]`, kann eine Pflicht-Rolle nie leeren); Shift-Klick (Desktop, geprüft über `event.shiftKey`) oder Long-Press (Touch, über Pointer Events mit 500 ms Schwelle + 10 px Bewegungstoleranz in `wireAddGeste()`) lösen stattdessen den bisherigen Toggle-Add-Pfad (`onAdd`) aus, der wie zuvor blockiert bleibt, wenn er die letzte Zutat einer Pflicht-Rolle entfernen würde. Eine Falle dabei gefunden und behoben: `renderMorphbox` baut bei jeder Zustandsänderung alle Options-Buttons per `root.replaceChildren()` neu — ein Flag direkt auf dem gedrückten Button (`dataset.longPress`) wäre nach dem durch `onAdd` ausgelösten Re-Render bereits verloren, bevor der nachfolgende synthetische Klick (nach `touchend`) ankommt, und hätte die Auswahl sofort wieder ersetzt. Gelöst mit einem Modul-weiten Flag (`unterdrueckeNaechstenKlick`) statt eines Elements-Attributs, plus 700-ms-Sicherheitsnetz falls der erwartete Klick ausbleibt. Verifiziert im Browser: Klick ersetzt (Speisestärke statt Weizenmehl), Shift-Klick mischt hinzu und entfernt wieder (Toggle), Shift-Klick auf die letzte Pflicht-Zutat bleibt wirkungslos, simulierter Long-Press (Pointer Events, realistisches Timing) mischt hinzu und der nachfolgende synthetische Klick auf den frisch gerenderten Button ändert die Auswahl nicht mehr — keine Konsolenfehler, keine Regression (29 bisherige Node-Checks weiterhin grün, von T30 unberührt). | — |
| T29 | Kandidatenliste + Error-Request (Schritt 3/3) | done | Neuer Toggle-Button „Regel-Kandidaten“ (wie „Korpus-Statistik“) öffnet Sektion `#kandidaten`. Neue statische Katalog-Funktion `alleUeberschreibbarenRegeln(data)` in `rules.js` durchsucht `operations.json` + die inline definierten Operationen in `methods.json` nach allen `hart`-Voraussetzungen mit `id`, unabhängig vom aktuellen Rezept (nötig, weil die Kandidatenliste unabhängig von der gerade gewählten Zutatenkombination sein muss). Zwei Evidenzquellen in einer Liste (`src/ui/kandidaten.js`): (a) Regeln mit ≥ 2 „hat funktioniert“-Antworten aus den T28-Overrides gelten als Kandidat; (b) ein Formular (Regel-Auswahl + Begründung) reicht direkt einen Error-Request ein, gespeichert im neuen Modul `src/model/errorRequests.js` (eigener `localStorage`-Schlüssel, gleiches Muster wie `overrides.js`), mit „Entfernen“-Option für eigene Einträge. Beides ist reine Anzeige/Erfassung — die App kann `operations.json` nicht selbst schreiben (statische Seite, kein Backend), die eigentliche Herabstufung bleibt manuelle Maintainer-Arbeit, wie schon die Korpus-Pflege. Verifiziert im Browser: Katalog listet alle 5 überschreibbaren Regeln korrekt; ein eingereichter Error-Request erscheint sofort und lässt sich entfernen; zwei simulierte „ja“-Overrides für dieselbe Regel lösen den Feedback-Kandidaten-Hinweis „2 von 3 Overrides haben funktioniert“ aus — keine Konsolenfehler, keine Regression bei T22/T24/T25/T26 (30 Node-Checks weiterhin grün). Damit ist der komplette Override+Feedback-Loop aus DR-019 Punkt 4 umgesetzt (T25→T28→T29); nur T23 (Erfahrungs-Regeln, blockiert auf T04) bleibt aus dem ursprünglichen DR-019-Konzept offen. | T28 |
| T28 | Rückfrage-UI für offene Overrides (Schritt 2/3) | done | Neue Sektion `#rueckfragen` (oberhalb des morphologischen Kastens, unabhängig von der aktuellen Zutatenauswahl) zeigt für jeden `localStorage`-Eintrag mit `ergebnis:'offen'` die Frage „Hat es funktioniert?“ (Ja/Nein-Buttons), zusammen mit dem beim Override gespeicherten Erklärtext + Kontext. Grenze `sessionStart` (einmal beim App-Start erfasst): nur Overrides von VOR diesem Seitenaufruf werden gefragt — ein gerade eben in dieser Sitzung gesetzter Override taucht erst beim nächsten Laden auf (sonst würde direkt nach dem Klick "hat's funktioniert?" gefragt, bevor überhaupt gebacken wurde). `overrides.js` um `overrideAntwort(regelId, zeitpunkt, ergebnis)` erweitert; `overrideErfassen` speichert jetzt zusätzlich den Erklärtext (`text`), damit die Rückfrage ihn ohne Neuberechnung anzeigen kann. Verifiziert im Browser: mit einem simulierten "alten" Override erscheint die Frage sofort beim Laden, Klick auf "Ja" schreibt `ergebnis:'ja'` in `localStorage` und blendet die Sektion aus; ein in der aktuellen Sitzung frisch gesetzter Override erscheint bewusst NICHT, erst nach einem vollständigen Reload — keine Konsolenfehler, alle 30 bisherigen Node-Checks (T22/T24/T25/T26) weiterhin grün. Siehe DR-019. | T25 |
| T25 | Override erfassen (Schritt 1/3 zu Feedback-Loop) | done | Alle fünf `hart`-Voraussetzungen im Datensatz (Natron-Säure an `backen`, Fett fest/flüssig an `schmelzen`/`verquirlen`, Fett fest/cremig an creme's `aufschlagen`) bekamen ein stabiles `id`-Feld. `rules.js` sammelt daraus in `resolveMethod` eine strukturierte `blockaden`-Liste ({id, ausloeser, text}, dedupliziert), durchgereicht als `computed.blockaden`. Neues Modul `src/model/overrides.js` kapselt `localStorage`-Zugriff (Schlüssel `chefing_overrides_v1`, still scheiternd bei deaktiviertem Storage — kein Blocker für die App). `recipeCard.js` zeigt pro Blockade ohne offenen Override einen Button „Trotzdem verwenden (merken)“; Klick speichert `{zeitpunkt, kontext (aktuelle Zutatenauswahl als Text), ergebnis:'offen'}` und erzwingt per `onChange({})` ein Re-Render, das den Button durch eine Bestätigungsnotiz ersetzt. Kein Freischalten — eine `invalid`-Rezeptur war schon vorher vollständig sichtbar/nutzbar, das ist reine Beweis-Erfassung für die Rückfrage in T28. Verifiziert per Node-Skript (3 Fälle: Natron-Blockade mit korrekter id, zwei dedupliziert bei Öl in Creme, keine Blockade bei gültiger Kombination) und im Browser: Klick schreibt den erwarteten Eintrag in `localStorage`, Notiz wechselt sofort zur Bestätigung, und nach vollständigem Seiten-Reload + erneuter Auswahl derselben Kombination erscheint direkt die Bestätigung statt des Buttons (Persistenz bestätigt) — keine Konsolenfehler, keine Regression bei T22/T24/T26 (30 Node-Checks weiterhin grün). Siehe DR-019-Nachtrag. | T22, T24 |
| T27 | Vorschlagen-dann-bestätigen-UI für Kaskade (Schritt 3/3) | done | `recipeCard.js` zeigt jeden in T26 berechneten Ersatzvorschlag als eigene Notiz („Vorschlag: Buttermilch statt Milch bei „Flüssigkeit" verwenden — löst „Backen".") mit „Übernehmen"-Button; erst der Klick ersetzt die aktuelle Auswahl der Zielrolle vollständig durch den Kandidaten (`onChange({ zutaten: { ...state.zutaten, [rolle]: [{zutat, anteil:1}] } })`) — vorher keine State-Änderung. `renderRecipe` bekam dafür `state`/`data`/`onChange` als neue Parameter (vorher nur `computed`). Damit ist die „Kaskaden-Propagation" aus DR-019 vollständig umgesetzt (T22 Physik-Regeln in Daten → T24 Wert-Zustand/Pin → T26 Zielrolle+Kandidat → T27 sichtbare Übernahme). Verifiziert im Browser: Natron ohne Säure zeigt den Vorschlag, Klick auf „Übernehmen" ersetzt Milch durch Buttermilch und macht die Crememethode gültig; ist die Zielrolle (Flüssigkeit) stattdessen fixiert, erscheint gar kein Vorschlag (T24-Konfliktregel bleibt gewahrt) — keine Konsolenfehler, keine Regression bei T22/T24/T26 (24 Node-Checks weiterhin grün). | T26 |
| T26 | Kaskaden-Zielrolle + Ersatzkandidat (Schritt 2/3) | done | Neues optionales Feld `kaskade_ziel` an `voraussetzung`-Einträgen (`operations.json`) — bei der Natron-Säure-Regel auf `fluessigkeit` gesetzt. `rules.js`: `findKaskadeKandidat()` sucht die erste Option der Zielrolle (in Deklarationsreihenfolge aus `archetypes.json`), die die geforderte Eigenschaft erfüllt (z. B. `saeure: true` → Buttermilch, erste saure Option nach Milch); nur für `hart`-Bedingungen, nur wenn die Zielrolle nicht `gesetzt-fix` ist (T24-Konfliktregel), dedupliziert. Ergebnis landet als `kaskaden`-Array in `resolveMethod`/`evaluateAll` und wird in `engine.js` als `computed.kaskaden` durchgereicht — reine Berechnung, keine State-Mutation, keine UI (die Übernahme-UI kommt in T27). Verifiziert per Node-Skript (5 Fälle: Vorschlag korrekt, Zielrolle fixiert → kein Vorschlag, andere Rolle fixiert → Vorschlag bleibt, kein Trigger, bereits gelöst) und live im Browser per Modul-Import gegen den echten Fetch-geladenen Datensatz (`computed.kaskaden` enthält den erwarteten Buttermilch-Vorschlag), keine Konsolenfehler. Siehe DR-019. | T24 |
| T24 | Wert-Zustand (Schritt 1/3 zur Kaskade) | done | Neues `state.fixiert[rolle] = true`-Feld (bewusste Abweichung vom ursprünglichen Todo-Text `state.zutaten[rolle].zustand`: ein Sibling-Feld statt einer Erweiterung des bestehenden Array-Schemas hält `propsOf`/`engine.js`/die Anteile-Logik komplett unangetastet, KISS). Pin-Icon (📌) je Zutaten-Rolle in `morphbox.js`, togglebar, setzt/löscht den Eintrag; wird bei Preset-Wechsel zurückgesetzt (neue Zutaten-Grundlage würde sonst sofort mit alten Fixierungen kollidieren). `fixiert` läuft als neuer Parameter durch `evaluateAll`/`resolveMethod`/`checkOperation` in `rules.js`: schlägt eine `hart`-Voraussetzung fehl UND ist die verantwortliche Rolle fixiert (direkt die Bedingungs-Rolle, oder bei `wenn`-Bedingungen die Rolle, deren fixierte Wahl die Voraussetzung erst auslöst, z. B. Trieb=Natron), hängt `beschreibeBedingungen` einen eigenen Satz an („‚Trieb' ist bewusst fixiert 📌 und wird deshalb nicht automatisch geändert") statt still zu bleiben — noch keine automatische Ersatzsuche (T26). Verifiziert per Node-Skript (5 Fälle: ohne Fixierung, Trieb fixiert, falsche Rolle fixiert bleibt stumm, direkte Rolle fixiert) und im Browser (Pin an Trieb → Meldungstext ändert sich; Preset-Wechsel setzt alle Pins zurück), keine Konsolenfehler. Siehe DR-019-Nachtrag. | T22 |
| T22 | Physik-Regeln aus Code in Daten migrieren | done | Beide Sonderfälle aus DR-002s Risiko sind jetzt generisch aus `operations.json` gelesen statt an Schlüsselnamen im Code festgemacht. (1) Natron-Säure-Check: neue Bedingungsform in `rules.js` — `rolle: "!x"` prüft existenziell „irgendeine Rolle außer x“, `wenn` macht eine Voraussetzung guard-abhängig, `hinweis` liefert bei Bedarf einen festen Erklärtext statt der generischen Formel; die Regel selbst steht jetzt als `voraussetzung` an der Operation `backen`. (2) `nachVorbereitung` (Fett wird nach Schmelzen flüssig) liest jetzt `wirkt_auf_eigenschaften` aus der jeweiligen Vorbereitungs-Operation, statt den Effekt am String `'schmelzen'` festzumachen. Verifiziert per Node-Skript (7 Fälle: Natron ohne/mit Säure, Backpulver ohne Guard-Auslösung, Schmelzen-Auto-Insert) und im Browser (Natron ohne Buttermilch → „Backen“ blockiert mit Grund; Buttermilch dazu → Crememethode wieder gültig), keine Konsolenfehler. Siehe DR-019. | — |
| T19 | Icon-Set überarbeiten: freies Icon-Set prüfen + eigene Nacharbeiten + Outline-Variante | done | Rechercheergebnis in DR-016: kein freies Icon-Set übernommen (Stilbruch + `sieben` existiert in keinem der Hunderte über Iconify aggregierten Sets). Stattdessen: `salz` = drei lose Körner statt Streuer, `suesse` = drei gestapelte Zuckerwürfel statt vier schwebender Quadrate, `schneiden` = diagonales Kochmesser mit Loch im Griff statt Dreieck+Block. `mischen` durchlief mehrere Nachträge (Twist → Löffel-Bögen → finale Version: 360°-Rotation auf gestauchter Ellipse, aus 3 Optionen gewählt). Neue optionale `variant: 'holo'` (Umriss statt Vollfläche) in `iconHtml`/`iconGroup`, generisch aus denselben Formen abgeleitet; kleine Körner-/Streugut-Punkte (`salz`, `pulver`, `sieben`) tragen `keepFill: true` und bleiben in „holo" gefüllt statt zu Ringen zu werden. `dev/icons-preview.html` mit großer Detail-Ansicht + Fallback-Hinweis, falls die Seite ohne lokalen Server (`file://`) geöffnet wird. Das ehemalige `garen`-Icon (Heizspirale) wurde danach in DR-017 zu einer Flamme unter dem verallgemeinerten Verb `erhitzen` — siehe dort, nicht mehr Teil von T19. Verifiziert per Vorschau-Seite + im Browser (App) + direkter DOM-Prüfung. | T11 |
| T20 | Copyright der Datenquellen (Korpus) prüfen | done | Rechercheergebnis in DR-015: Zutatenmengen/-listen sind Fakten, nicht schutzfähig — frei nutzbar. Zubereitungsanleitungen wären nur bei „Schöpfungshöhe“ geschützt, unsere kurzen `ablauf`-Paraphrasen erreichen die laut Rechtsprechung ohnehin nicht — und werden im UI gar nicht ausgespielt (nur eigene `methods.json`-Schritte + Quellenlink/Sitename/Bewertung in `stats.js`). Keine Bilder im Korpus. Einziges Risiko: Datenbankschutz bei zu vielen Rezepten aus derselben Domain (siehe Hinweis bei T04). Keine Code-Änderung nötig. | – |
| T18 | Icon-Set für Rühr-Verben konsolidieren (mix in, blend, unterheben) | done | Umgesetzt als KISS-Variante (DR-014, Nachtrag): `zusatzverben`-Feld in `operations.json`, `unterheben_abwechselnd` trägt `zusatzverben: ["sieben", "wiederholen"]` — ein Schritt, drei Icons, statt separatem Sieben- oder Wiederholen-Schritt. `muffin`/`allinone` unverändert. Im Browser verifiziert (beide Methoden, keine Konsolenfehler). | – |
| T17 | Deploy per `deploy.php` auf eigenes Hosting (DR-013) | done | `deploy.php` + `chefing.cfg` live auf hamsch.net, `.htaccess` blockt `*.cfg` (403 verifiziert), Deploy-Aufruf getestet (22 Dateien geschrieben, 0 Fehler). | – |
| T16 | Quellen-Log für Web-Recherche | done | `docs/QUELLEN.md` angelegt, rückwirkend befüllt (11 Domains, 21 Seiten aus dem bestehenden Korpus). Konvention als Memory hinterlegt: jede künftige Recherche wird dort angehängt (pro Domain, nicht pro Einzelseite). | T04 |
| T15 | Multi-Zutat je Rolle mit Anteilen (Option B, DR-012) | done | `state.zutaten[rolle]` ist jetzt `[{zutat, anteil}]` statt einer einzelnen Zutat — für ALLE Rollen (Nutzer mischt z. B. Fett aus Butter+Öl). `propsOf` blendet Eigenschaften (dominante Zutat + Oder-Verknüpfung für saeure/bildet_gluten/quellfaehig/trieb_typ), `engine.js` verteilt die Rollen-Bäckerprozent nach Anteilen auf mehrere Zeilen, `morphbox.js` erlaubt Mehrfachauswahl mit Anteil-Eingabe. Ersetzt die enger gefasste Rollen-Split-Idee aus DR-011. | T14 |
| T14 | Regression: Gluten-Warnung prüfte falsches Feld | done | `rules.js` prüfte `kategorie !== 'getreide'`, das seit DR-008 bei allen Struktur-Optionen „mehl“ ist → Warnung feuerte immer, auch bei reinem Weizenmehl. Fix: neues Feld `bildet_gluten` je Zutat, Warnung präzisiert (Bindung vs. Körper, mit Korpus-Beispiel). Verifiziert: Weizenmehl ohne Warnung, Speisestärke mit korrektem Text. Siehe DR-010. | T08 |
| T13 | Eigenes „Sieben“-Verb für trockenes Mischen | done | Siebtes Verb `sieben` (Sieb-Ring + durchfallendes Pulver) für `trocken_mischen`, statt des generischen `mischen`-S. Verifiziert per Icon-Vorschau + Ablaufdiagramm. Siehe DR-009-Nachtrag 2. | T12 |
| T12 | Icon-Feinschliff: S-Twist, Steak, eigenes Whisking-Verb | done | `mischen` = S-Twist-Bogen, `fleisch` = Steak mit ausgesparten Grillstreifen (evenodd), neues Verb `schlagen` (Schneebesen) für aufschlagen/verquirlen/eier_zucker_schaumig. Verifiziert per Icon-Vorschau + App-Ablaufdiagramm. Siehe DR-009-Nachtrag. | T11 |
| T11 | Piktogramme statt Emoji + Gemüse/Obst-Split + Schritt = Zutat+Verb (DR-009) | done | `src/ui/icons.js` (handgezeichnete Formen, HTML- und SVG-Renderer), `gemuese`/`obst` getrennt, Ablaufschritt zeigt Zutat-Kategorie-Icons + Verb-Icon kombiniert. Im Browser verifiziert (Icon-Vorschau-Seite + App, alle drei Presets, keine Konsolenfehler). | T08 |
| T09 | Operationen echt auf Verb+Intensität verdichten | done | Umgesetzt (DR-014, 2. Nachtrag): `verb` auf 4 Werte reduziert (mischen/sieben/formen/garen), `intensitaet` (passiv/sanft/kraeftig) + `verteilung` (einzeln/abwechselnd) als neue kleine Attribute auf `verb: mischen`. 6 methodenspezifische Operationen (aufschlagen, eier_einzeln, eier_zucker_schaumig, trocken_mischen, unterheben_kurz, alles_verruehren) aus `operations.json` entfernt und inline in die jeweilige `methods.json`-`sequenz` verschoben (`sequenz`-Einträge dürfen jetzt String oder Objekt sein). `operations.json` von 13 auf 7 benannte Operationen geschrumpft. `rules.js` löst beide Formen gleich auf; `engine.js`/`flow.js` unverändert bis auf 2 durchgereichte Felder + Icon-Ableitungstabelle. Alle 4 Methoden im Browser verifiziert. | T08 |
| T08 | Kategorien + Verben + Strenge-Grade (DR-008) | done | `kategorien.json` (10 Kategorien, Icons), `verben.json` (5 Verben, Icons), `ingredients.json`/`operations.json` erweitert, `rules.js` prüft `strenge` (hart/empfohlen/frei), Icons in Morph-Kasten + Ablaufdiagramm. Verifiziert: Öl + All-in-one jetzt gültig mit Empfehlungs-Hinweis (vorher hart blockiert); Öl + Creme weiterhin ungültig (harte Regel). | T01 |
| T02 | Push nach GitHub | done | `c809b84` und `f667e53` sind auf `origin/main`. Nutzer hat SSH-Remote eingerichtet und gepusht. | T01 |
| T01 | MVP Rührteig-Kombinator | done | Generisches Schema, Regeln in `operations.json`, Korpus (3×7 Rezepte), Morph-Kasten, Ablaufdiagramm, Rezeptkarte, Statistik. Commit `c809b84`. | – |

<!-- Neue done-Einträge oben (direkt unter der Tabellen-Kopfzeile) einfügen, nicht ans Ende. -->
